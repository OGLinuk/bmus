# (B)ack (M)e (U)p (S)cotty

## Usage

```go build```

```./bmus -f <desired_flags> -t <backup_target> -d <backup_destination>```

## Todo
* Add encryption option for backup file
* Add different options for backup archival
* Add ssh option for rsync